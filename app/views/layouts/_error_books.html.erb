<% if model.errors.any? %>
  <div class="alert alear-warning">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this product from being saved:</h2>
  　<ul>
    <% model.errors.full_messages.each do |book| %>
    <li><%= book %></li>
    <% end %>
    </ul>
  </div>
<% end %>